import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.zimbabwe.numberplatesystem',
  appName: 'ENP System',
  webDir: 'dist'
};

export default config;
